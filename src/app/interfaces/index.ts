export interface IResponse {
  response: string
  success: boolean
}

export interface Imodal {
  description: string
  title: string
}
export enum EnumType {
  AHORRO,
  GASTO,
  SALARIO,
  IGRESON
}